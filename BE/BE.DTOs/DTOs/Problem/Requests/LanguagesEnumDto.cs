namespace BE.DTOs.DTOs.Problem.Requests;

public enum LanguagesEnumDto
{
    // we will have a select with the value set as the enum index and between the option tags we will have the enum name

    // C# language id,
    CSharp = 51
}